<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>ForgeHire | The Future of Hiring</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body { scroll-behavior: smooth; }
  </style>
</head>

<body class="bg-gray-50 text-gray-900 font-sans">

  <!-- Hero Section -->
  <section class="text-center py-20 px-4">
    <h1 class="text-4xl font-bold mb-4">The Future of Hiring Starts Here</h1>
    <p class="text-lg text-gray-600 mb-8">
      Get AI-powered Job Descriptions in seconds and early access to ForgeHire — our upcoming AI hiring platform.
    </p>
    <div class="flex flex-col sm:flex-row justify-center gap-4">
      <a href="#jd-generator" class="bg-blue-600 text-white px-6 py-3 rounded-xl hover:bg-blue-700 transition">
        Try Free JD Generator
      </a>
      <a href="#early-access" class="border border-blue-600 text-blue-600 px-6 py-3 rounded-xl hover:bg-blue-50 transition">
        Join Early Access
      </a>
    </div>
  </section>

  <!-- JD Generator Form -->
  <section id="jd-generator" class="max-w-xl mx-auto bg-white shadow-md rounded-2xl p-8 mb-20">
    <h2 class="text-2xl font-semibold mb-4 text-center">Free AI Job Description Generator</h2>
    <p class="text-center text-gray-500 mb-4">Powered by OpenAI • Integrated with Make.com</p>

    <form id="jdForm" class="space-y-3">
      <input type="text" id="company" placeholder="Company Name" class="w-full p-3 border rounded-xl" required>
      <input type="text" id="role" placeholder="Job Role / Title" class="w-full p-3 border rounded-xl" required>
      <select id="type" class="w-full p-3 border rounded-xl">
        <option>Full-time</option><option>Part-time</option><option>Contract</option>
      </select>
      <input type="text" id="location" placeholder="Location (e.g., Remote / Dhaka)" class="w-full p-3 border rounded-xl" required>
      <input type="text" id="experience" placeholder="Experience Level (e.g., Junior)" class="w-full p-3 border rounded-xl" required>
      <textarea id="responsibilities" placeholder="Key Responsibilities" class="w-full p-3 border rounded-xl"></textarea>
      <textarea id="requirements" placeholder="Key Requirements / Skills" class="w-full p-3 border rounded-xl"></textarea>
      <input type="email" id="email" placeholder="Your Email" class="w-full p-3 border rounded-xl" required>

      <button type="submit" class="w-full bg-blue-600 text-white py-3 rounded-xl hover:bg-blue-700 transition">Generate JD</button>
    </form>

    <div id="loading" class="text-center mt-4 hidden">⏳ Generating...</div>
    <div id="result" class="bg-gray-100 mt-4 p-4 rounded-xl hidden"></div>
  </section>

  <!-- Early Access Offer -->
  <section id="early-access" class="max-w-3xl mx-auto text-center bg-gradient-to-r from-blue-50 to-blue-100 p-10 rounded-2xl shadow-sm mb-20">
    <h2 class="text-3xl font-semibold mb-4">Join ForgeHire Early Access 🚀</h2>
    <p class="text-gray-700 mb-6">
      Be among the first to experience our beta AI hiring platform.<br>
      Get <strong>3 free developer hiring competitions</strong> + exclusive beta features.
    </p>

    <!-- Embedded Tally Form -->
    <div class="w-full flex justify-center">
      <iframe data-tally-src="YOUR_TALLY_FORM_EMBED_LINK" width="100%" height="500" frameborder="0" marginheight="0" marginwidth="0" title="ForgeHire Early Access"></iframe>
    </div>
  </section>

  <footer class="text-center mt-10 text-gray-500 text-sm pb-10">
    ForgeHire Beta © 2025. All rights reserved.
  </footer>

  <!-- JS: Connect to Make Webhook -->
  <script>
    const form = document.getElementById('jdForm');
    const loading = document.getElementById('loading');
    const result = document.getElementById('result');

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      loading.classList.remove('hidden');
      result.classList.add('hidden');

      const data = {
        company: form.company.value,
        role: form.role.value,
        type: form.type.value,
        location: form.location.value,
        experience: form.experience.value,
        responsibilities: form.responsibilities.value,
        requirements: form.requirements.value,
        email: form.email.value
      };

      try {
        const res = await fetch('PASTE_YOUR_MAKE_WEBHOOK_URL_HERE', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(data)
        });
        const text = await res.text();
        result.innerText = text;
      } catch (err) {
        result.innerText = '❌ Error generating job description. Please try again.';
      }

      loading.classList.add('hidden');
      result.classList.remove('hidden');
    });
  </script>

  <script async src="https://tally.so/widgets/embed.js"></script>
</body>
</html>
